node_modules/
.next/
out/
dist/
build/
coverage/
test-results/
playwright-report/
next-env.d.ts
*.tsbuildinfo

# Prisma client is generated at build time from prisma/schema.prisma.
packages/engine-db/generated/

.env
.env.local
.DS_Store

# PRDs live in the INTERNAL stonedog-prd repo, never here. `docs/prd` is a
# symlink into it, so the documents are where a person expects while this
# repository commits none of them. See the StoneDogCode CLAUDE.md.
docs/prd
